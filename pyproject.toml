[project]
name = "tech-tree"
version = "0.1.0"
description = "AI-native paper & tech-blog learning pipeline"
requires-python = ">=3.11"
dependencies = [
    "feedparser>=6.0",
    "pyyaml>=6.0",
]

[dependency-groups]
site = [
    "mkdocs>=1.6,<2",
    "mkdocs-material>=9.5",
    "mkdocs-roamlinks-plugin>=0.3",
]

[tool.uv]
package = false
